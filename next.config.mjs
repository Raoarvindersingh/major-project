/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com'], // 👈 required for Google profile photos
      },
};

export default nextConfig;
